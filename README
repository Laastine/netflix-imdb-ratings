INSTALL:
In chrome, Wrench bar -> Tools -> Extensions
check the "Developer mode"
click "Load unpacked extension.." and select this source directory

KNOWN BUGS:
	-Doesn't fetch ratings for all movie title's correctly when using some other language than English

